{
  "name": "emoji-on-relay",
  "version": "1.0.0",
  "description": "WebSocket relay server for Emoji-On — routes messages between Godot host and web clients",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test src/**/*.test.js"
  },
  "type": "module",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
